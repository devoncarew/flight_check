{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | grep -q 'pubspec\\.yaml' && dart pub outdated 2>/dev/null || true",
            "statusMessage": "Checking for outdated packages..."
          }
        ]
      }
    ]
  }
}
